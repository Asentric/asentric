# Asentric Configuration
# Generated by: asentric init
# Documentation: https://github.com/asentric/asentric

version: "1.0"

# Chain Configuration
chain:
  id: 5003                                    # Mantle Sepolia
  name: "Mantle Sepolia"
  rpcUrl: "https://rpc.sepolia.mantle.xyz"    # HTTP RPC (optional)
  rpcWs: "wss://rpc.sepolia.mantle.xyz/ws"    # WebSocket RPC (required)

# Event Source Configuration
source:
  type: "websocket"                           # Options: websocket, memory
  url: "wss://rpc.sepolia.mantle.xyz/ws"

# Alert Sink Configuration
sink:
  type: "console"                             # Options: console, webhook
  url: ""                                     # Webhook URL (if type=webhook)

# Queue Configuration (MVP: memory only)
queue:
  type: "memory"
  url: ""

# ABI Configuration
abi:
  registryPath: "config/registry.yaml"

# Debug Mode
debug: true
